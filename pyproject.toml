[project]
name = "starfield-handle-parser"
version = "0.1.0"
description = "Parse Starfield DumpPointerHandles logs using real full, medium, and light plugin ownership"
requires-python = ">=3.14"
dependencies = []

[tool.pytest.ini_options]
minversion = "9.0"
testpaths = ["tests"]
addopts = ["-q", "--tb=short"]
